
import React from 'react';
import { MessageCircle } from 'lucide-react';

const ChatIcon = () => {
  // Hide chat icon for now since there's no authentication
  return null;
};

export default ChatIcon;
